/**
 * Internal metadata key used to expose tool return schema to Monty preamble builders.
 * Expects: attached as a non-enumerable property on Tool objects.
 */
export const MONTY_RESPONSE_SCHEMA_KEY = "xResponseSchema";
